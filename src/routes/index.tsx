import { createFileRoute } from '@tanstack/react-router'
import PollinationServices from '@/pages/PollinationServices'

export const Route = createFileRoute('/')({
    component: PollinationServices,
})
